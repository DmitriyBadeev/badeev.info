query works {
    works {
        nodes {
            id
            title
            shortDescription
            date
            imgPath
        }
    }
}

query Count {
    works {
        totalCount
    }
}

query tags {
    tags {
        id
        title
    }
}
