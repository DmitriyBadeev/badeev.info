query works {
    works {
        nodes {
            id
            title
            shortDescription
            date
            imgPath
        }
    }
}

query tags {
    tags {
        id
        title
    }
}
