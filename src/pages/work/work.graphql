query workById($id: Int!) {
    workById(id: $id) {
        id
        title
        date
        imgPath
        html
        link
        task
        authors {
            id
            name
            link
            role
        }
    }
}
