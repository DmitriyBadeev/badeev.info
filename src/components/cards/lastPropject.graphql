query lastWorks($count: PaginationAmount) {
    works(first: $count) {
        nodes {
            id
            title
            shortDescription
            date
            imgPath
        }
    }
}
